/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: { esmExternals: false, },
    // output: 'export',
    images: {
        domains: ["img.freepik.com", 'media.istockphoto.com', "i.ibb.co"]
    }
}

module.exports = nextConfig
